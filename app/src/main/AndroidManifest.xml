<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RafidTask"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="tiramisu" android:usesCleartextTraffic="true">
        <activity
            android:name=".view.ServerErrorActivity"
            android:exported="true" />
        <activity
            android:name=".view.BookWishlistRecyclerView"
            android:exported="true" />
        <activity
            android:name=".view.BookDetailsActivity"
            android:exported="true" />
        <activity
            android:name=".view.BookSearchResultRecyclerView"
            android:exported="true" />
        <activity
            android:name=".view.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>